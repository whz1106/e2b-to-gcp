# Codex Artifacts

这个目录用于集中放置本轮整理出来的新增内容，避免把试验性文档和脚本散落到仓库主线目录中。

目录说明：

- `docs/`: 新增的中文说明文档
- `kits/`: 试验性脚本、验证脚本、自建模板示例

约束：

- 这里的内容是辅助材料，不属于仓库原有的主线部署代码
- `packages/shared`、`iac/`、主线 `Makefile` 等部署相关代码已恢复，不在这里继续改动
